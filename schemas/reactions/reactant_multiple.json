{
  "$schema": "http://json-schema.org/schema#",

  "$id" : "http://github.com/NCAR/MechanismToCode/schemas/reactions/reactant_multiple.json",
  "type" : "object",
  "description" : "A reactant that can be present multiple times in a reaction. For example, NO + NO + O2 -> NO2 + NO2",
  "example" : {
    "type" : "NO",
    "number" : 2
  },
  "properties" : {
    "species" : {
      "type" : "string",
      "description" : "Species name. This species must be present in the set of chemical species this reaction is applied to. Specific reactions may require certain species properties to also be present."
    },
    "number" : {
      "type" : "integer",
      "description" : "The number of a particular species participating in a reaction. For self reactions this number would be 2.",
      "default" : 1
    }
  },
  "additionalProperties" : false,
  "required" : [ "species" ]
}
